# CKCP LAMP Flutter - 跨平台架构设计文档

## 📋 项目概述

**CKCP LAMP Flutter** 是一个基于 Flutter 框架的跨平台车载氛围灯控制与OTA升级应用，支持 iOS、Android、macOS、Windows、Linux 五大平台。

本项目参考 WebOTA 项目的功能和协议，实现完整的 BLE 设备连接、氛围灯控制、OTA 固件升级等功能。

---

## 🏗️ 系统架构

### 分层架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                        │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐         │
│  │  Pages  │  │ Widgets │  │ Themes  │  │  i18n   │         │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘         │
└───────┼────────────┼────────────┼────────────┼──────────────┘
        │            │            │            │
        ▼            ▼            ▼            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Business Logic Layer                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   BLoC/     │  │   State     │  │  Use Cases  │          │
│  │   Riverpod  │  │  Management │  │  (Actions)  │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
└─────────┼────────────────┼────────────────┼─────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────┐
│                    Domain Layer                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   Entities  │  │ Repositories│  │  Protocols  │          │
│  │   (Models)  │  │ (Abstract)  │  │  (Commands) │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
└─────────┼────────────────┼────────────────┼─────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────┐
│                    Data Layer                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │    BLE      │  │    Local    │  │   Platform  │          │
│  │   Service   │  │   Storage   │  │   Adapters  │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────┐
│                    Platform Layer                            │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐ │
│  │  iOS   │  │Android │  │ macOS  │  │Windows │  │ Linux  │ │
│  └────────┘  └────────┘  └────────┘  └────────┘  └────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 项目目录结构

```
ckcp_lamp_flutter/
├── lib/
│   ├── main.dart                    # 应用入口
│   ├── app.dart                     # App 根组件配置
│   │
│   ├── core/                        # 核心功能模块
│   │   ├── constants/               # 常量定义
│   │   │   ├── ble_uuids.dart       # BLE UUID 常量
│   │   │   ├── colors.dart          # 预设颜色常量
│   │   │   └── commands.dart        # 协议命令常量
│   │   │
│   │   ├── protocols/               # 协议层
│   │   │   ├── ckcp_protocol.dart   # CKCP 协议核心
│   │   │   ├── ambient_commands.dart # 氛围灯命令生成
│   │   │   ├── ota_protocol.dart    # OTA 升级协议
│   │   │   └── protocol_parser.dart # 协议解析器
│   │   │
│   │   ├── services/                # 服务层
│   │   │   ├── ble_service.dart     # BLE 连接服务
│   │   │   ├── ota_service.dart     # OTA 升级服务
│   │   │   └── storage_service.dart # 本地存储服务
│   │   │
│   │   ├── utils/                   # 工具类
│   │   │   ├── hex_utils.dart       # 十六进制工具
│   │   │   ├── color_utils.dart     # 颜色工具
│   │   │   └── logger.dart          # 日志工具
│   │   │
│   │   └── extensions/              # Dart 扩展
│   │       └── string_extensions.dart
│   │
│   ├── domain/                      # 领域层
│   │   ├── entities/                # 实体/模型
│   │   │   ├── device_info.dart     # 设备信息
│   │   │   ├── light_state.dart     # 灯光状态
│   │   │   ├── led_config.dart      # LED 配置
│   │   │   └── ota_progress.dart    # OTA 进度
│   │   │
│   │   └── repositories/            # 仓库接口
│   │       ├── ble_repository.dart
│   │       └── settings_repository.dart
│   │
│   ├── presentation/                # 表现层
│   │   ├── providers/               # 状态管理 (Riverpod)
│   │   │   ├── ble_provider.dart    # BLE 状态
│   │   │   ├── light_provider.dart  # 灯光控制状态
│   │   │   ├── ota_provider.dart    # OTA 状态
│   │   │   └── settings_provider.dart
│   │   │
│   │   ├── pages/                   # 页面
│   │   │   ├── home_page.dart       # 主页
│   │   │   ├── ambient_light_page.dart  # 氛围灯控制页
│   │   │   ├── ota_page.dart        # OTA 升级页
│   │   │   └── factory_page.dart    # 工厂模式页
│   │   │
│   │   ├── widgets/                 # 通用组件
│   │   │   ├── common/              # 基础组件
│   │   │   │   ├── glass_card.dart  # 毛玻璃卡片
│   │   │   │   ├── custom_button.dart
│   │   │   │   ├── switch_card.dart # 开关卡片
│   │   │   │   └── slider_control.dart
│   │   │   │
│   │   │   ├── color_picker/        # 颜色选择器
│   │   │   │   ├── hsl_color_picker.dart
│   │   │   │   └── preset_colors.dart
│   │   │   │
│   │   │   ├── device/              # 设备相关
│   │   │   │   ├── device_card.dart
│   │   │   │   └── connection_status.dart
│   │   │   │
│   │   │   └── ota/                 # OTA 相关
│   │   │       ├── progress_indicator.dart
│   │   │       └── firmware_picker.dart
│   │   │
│   │   └── themes/                  # 主题
│   │       ├── app_theme.dart       # 主题配置
│   │       ├── colors.dart          # 颜色系统
│   │       └── typography.dart      # 字体排版
│   │
│   └── l10n/                        # 国际化
│       ├── app_localizations.dart
│       ├── intl_en.arb
│       └── intl_zh.arb
│
├── assets/                          # 资源文件
│   ├── images/
│   │   └── logo.png
│   ├── fonts/
│   └── i18n/
│
├── test/                            # 测试
│   ├── unit/
│   ├── widget/
│   └── integration/
│
├── windows/                         # Windows 平台配置
├── macos/                           # macOS 平台配置
├── linux/                           # Linux 平台配置
├── ios/                             # iOS 平台配置
├── android/                         # Android 平台配置
│
├── pubspec.yaml                     # 依赖配置
└── README.md
```

---

## 🔌 核心依赖

### pubspec.yaml 核心依赖

```yaml
dependencies:
  flutter:
    sdk: flutter
  
  # 状态管理
  flutter_riverpod: ^2.4.9
  
  # BLE 蓝牙
  flutter_blue_plus: ^1.31.13      # 移动端 (iOS/Android)
  win_ble: ^4.0.0                  # Windows 专用
  quick_blue: ^0.6.0               # 跨平台备选
  
  # 本地存储
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # UI 组件
  flutter_colorpicker: ^1.0.3
  fl_chart: ^0.66.0
  lottie: ^3.0.0
  
  # 文件处理
  file_picker: ^6.1.1
  path_provider: ^2.1.2
  
  # 国际化
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1
  
  # 工具类
  logger: ^2.0.2+1
  equatable: ^2.0.5
```

---

## 🔧 跨平台 BLE 适配策略

### 各平台 BLE 支持情况

| 平台    | BLE 库              | 说明                           |
|---------|---------------------|--------------------------------|
| iOS     | flutter_blue_plus   | 完整 CoreBluetooth 支持         |
| Android | flutter_blue_plus   | 完整 Android BLE 支持           |
| macOS   | flutter_blue_plus   | CoreBluetooth 支持              |
| Windows | win_ble             | Windows.Devices.Bluetooth API  |
| Linux   | quick_blue          | BlueZ D-Bus API                |

### BLE 服务抽象接口

```dart
abstract class BleServiceAdapter {
  Stream<BleDevice> scanDevices({Duration timeout});
  Future<void> connect(String deviceId);
  Future<void> disconnect();
  Future<void> sendData(Uint8List data);
  Stream<Uint8List> get notifications;
  bool get isConnected;
}
```

---

## 📡 CKCP 协议映射

参考 WebOTA 项目的 `AmbientProtocol.js`，完整映射到 Dart：

### 命令常量

```dart
class CkcpCommand {
  static const int singleColor = 0x01;           // 单色模式
  static const int zoneBrightnessSwitch = 0x02;  // 区域亮度开关
  static const int brightness = 0x03;            // 亮度调节
  static const int lightSwitch = 0x04;           // 开关控制
  static const int dynamicMode = 0x05;           // 动态/静态模式
  static const int multiTheme = 0x06;            // 多色主题
  static const int syncMode = 0x07;              // 同步模式
  static const int diyChannel = 0x08;            // DIY 通道
  static const int rhythmTheme = 0x09;           // 律动主题
  static const int rhythmSpeed = 0x0A;           // 律动速度
  
  // LED 配置 (0x0D - 0x13)
  static const int ledConfigMain = 0x0D;         // 主驾
  static const int ledConfigCoPilot = 0x0E;      // 副驾
  
  // 查询与系统
  static const int query = 0xFC;                 // 查询命令
  static const int factoryMode = 0xFE;           // 工厂模式
  static const int factoryReset = 0xFF;          // 出厂重置
  
  // OTA
  static const int upgrade = 0xD8;               // OTA 升级
}
```

---

## 🎨 UI/UX 设计规范

### 设计风格

1. **玻璃态 (Glassmorphism)** - 半透明毛玻璃效果
2. **暗黑模式** - 深色背景，护眼且高级
3. **流畅动画** - 微交互动画提升用户体验
4. **响应式布局** - 适配不同屏幕尺寸

### 颜色系统

```dart
class AppColors {
  // 主色调
  static const primary = Color(0xFF6366F1);      // Indigo
  static const secondary = Color(0xFFA855F7);    // Purple
  static const accent = Color(0xFF06B6D4);       // Cyan
  
  // 背景
  static const bgDark = Color(0xFF0F172A);       // Slate 900
  static const bgCard = Color(0x1AFFFFFF);       // 10% White
  
  // 状态
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);
}
```

---

## 🚀 开发路线图

### Phase 1: Windows 首发 ✅ (已完成)
- [x] 项目架构搭建
- [x] 核心协议层实现
- [x] Windows BLE 适配 (win_ble)
- [x] 基础 UI 组件
- [x] 氛围灯控制功能
- [x] OTA 升级功能

### Phase 2: 移动端适配
- [ ] Android 平台适配
- [ ] iOS 平台适配
- [ ] 移动端 UI 优化

### Phase 3: 桌面端扩展
- [ ] macOS 平台适配
- [ ] Linux 平台适配
- [ ] 桌面端窗口管理

### Phase 4: 高级功能 ✅ (部分完成)
- [x] 工厂模式完整实现
- [ ] 远程控制 (CAN/LIN)
- [ ] 数据日志导出
- [ ] 多设备管理
- [x] 国际化支持 (EN/ZH/JA)
- [x] Glassmorphism 2.0 UI 升级

---

## 📄 License

MIT License
